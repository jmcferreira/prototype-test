/// <summary>
/// Types of resources that units can spend to play cards.
/// Not all units use resources — it depends on the character class.
/// </summary>
public enum ResourceType
{
    None,
    Mana,
    Energy,
    Rage,
}
